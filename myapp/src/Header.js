import React from 'react'

// const headerStyle = {
//   backgroundColor: 'royalblue',
//   color : '#fff'
// };

const Header = ({title}) => {
  return (
    <header>
        <h1>{title}</h1>
    </header>
  )
}

export default Header